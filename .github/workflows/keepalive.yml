name: Keep SeraphBot Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://seraphbot.onrender.com/health > /dev/null
